<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="jp.msaitoappdev.caregiver.humanmed"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android 13+ 以降の通知ランタイム許可（下位は無視される） -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <application
        android:name=".CaregiverApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.CaregiverHumanMed">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- ランチャー -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 通知からは setClass(MainActivity) + data=caregiver://reminder で明示起動。
                 NavGraph の deepLink 定義が解決するため、Manifest での deep link 宣言は不要。 -->
        </activity>

        <!-- WorkManager はデフォルト初期化（カスタム Initializer がないため Manifest 追記不要） -->

    </application>
</manifest>
