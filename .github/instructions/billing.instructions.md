
# 課金（サブスクリプション）

## 適用範囲
- `:core` あるいは `:data` の課金関連、`PremiumRepository`/Billing 実装。

## 基本方針
- プレミアム状態は `PremiumRepository` が単一の**真実のソース**。`StateFlow<Boolean> isPremium` を公開し、全画面が購読。
- プレミアム特典：広告完全非表示、解説全文、1日の学習上限 10 セット、（将来）弱点特訓。

## 実装メモ
- 購入の復元機能を設定画面から提供。
- 将来はサーバー検証を検討（現状はクライアントのみ）。

